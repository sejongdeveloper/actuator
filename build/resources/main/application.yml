info:
  app:
    name: hello-actuator
    company: yh

management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true

  endpoint:
    shutdown:
      enabled: true
    health:
      show-components: always
  #      show-details: always
  endpoints:
#    jmx:
#      exposure:
#        include: "health,info"
    web:
      exposure:
        include: "*"
#        exclude: "env,beans"